 <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <%= link_to 'Go Back', movies_path, class: "nav-link" %>
      </li>
      <li class="nav-item active">
        <%= link_to 'New Comment', new_movie_comment_path(@movie), class: "nav-link" %>
      </li>
    </ul>
  </div>
</nav>

<%= link_to 'Edit Movie', edit_movie_path %>
<%= link_to 'Delete Movie - can only delete a movie you made', movie_path, method: :delete %>
 
 

<h3><%= @movie.title%></h3>
<h5><%= @movie.genre%></h5>
<p><%= @movie.description%></p>
<p><%= @movie.trailer%></p>

<% @movie.comments.each do |comment|%>
<%= link_to comment.body, movie_comment_path(@movie, comment)%>
<%= comment.user_id%>
<% end %>



